/*
 * Copy this file to ./server.conf.js and adjust the values as needed
 */
export const PORT = 3000;
// The path to where the table configurations and data resides
// The structure of the directory must be similar to table_examples where the subdirectories
// are treated as table names
export const TABLES_PATH = './table_examples';